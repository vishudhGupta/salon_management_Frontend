export class ApiConstants {
  //Exam for set the api end point to hit the backend
  //This is the base url for the api
    public static login: string = '/users/users/login';
    public static user_dashboard: string = '/users/users/dashboard/{{user_id}}';
}
